<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile</title>
  <!-- Bootstrap CSS -->
  <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css"
  />
  <!-- Google Fonts -->
  <link
    href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap"
    rel="stylesheet"
  />
  <style>
    /* Global Styles */
    body {
      background: linear-gradient(45deg, #f8f9fa, #e0eafc);
      font-family: "Roboto", sans-serif;
      padding-top: 80px;
      overflow-x: hidden;
    }

    /* Profile Container */
    .profile-container {
      background-color: #ffffff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      animation: fadeIn 1s ease-in-out;
    }

    /* Profile Header */
    .profile-header {
      text-align: center;
      margin-bottom: 40px;
      position: relative;
    }
    .profile-header img {
      border-radius: 50%;
      margin-bottom: 20px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 150px;
      height: 150px;
      object-fit: cover;
      animation: bounceIn 1s ease;
    }
    .profile-header h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
      font-weight: 700;
      color: #343a40;
    }
    .profile-header p {
      font-size: 1.2rem;
      color: #6c757d;
      font-weight: 400;
    }
    .edit-profile-button {
      position: absolute;
      top: 20px;
      right: 20px;
      padding: 10px 20px;
      font-size: 1rem;
      font-weight: 500;
      border-radius: 30px;
      transition: background-color 0.3s ease-in-out;
    }
    .edit-profile-button:hover {
      background-color: #007bff;
      color: #ffffff;
    }

    /* Profile Details */
    .profile-details {
      display: flex;
      justify-content: space-around;
      margin-bottom: 50px;
    }
    .profile-details .detail-box {
      background-color: #f1f3f5;
      padding: 20px;
      border-radius: 10px;
      width: 45%;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
    }
    .profile-details h3 {
      font-size: 1.5rem;
      margin-bottom: 10px;
      color: #495057;
      font-weight: 500;
    }
    .profile-details p {
      font-size: 2.5rem;
      color: #007bff;
      font-weight: 700;
      margin: 0;
    }

    /* Vehicles Section */
    .section-title {
      font-size: 2rem;
      font-weight: 600;
      color: #343a40;
      margin-bottom: 30px;
      border-left: 5px solid #007bff;
      padding-left: 10px;
    }
    .card {
      border: none;
      border-radius: 10px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 25px rgba(0, 0, 0, 0.1);
    }
    .card img {
      border-top-left-radius: 10px;
      border-top-right-radius: 10px;
      height: 200px;
      object-fit: cover;
    }
    .card-title {
      font-size: 1.25rem;
      font-weight: 600;
      color: #343a40;
    }
    .card-text {
      font-size: 1rem;
      color: #6c757d;
      margin-bottom: 15px;
    }
    .card-actions {
      display: flex;
      justify-content: space-between;
    }
    .card-actions .btn {
      font-size: 0.9rem;
      padding: 8px 20px;
      border-radius: 30px;
      font-weight: 500;
      transition: background-color 0.3s ease, color 0.3s ease;
    }
    .btn-edit {
      border: 1px solid #007bff;
      color: #007bff;
      background-color: transparent;
    }
    .btn-edit:hover {
      background-color: #007bff;
      color: #ffffff;
    }
    .btn-delete {
      border: 1px solid #dc3545;
      color: #dc3545;
      background-color: transparent;
    }
    .btn-delete:hover {
      background-color: #dc3545;
      color: #ffffff;
    }

    /* Reservations Section */
    .reservation-card {
      border-left: 5px solid #17a2b8;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      margin-bottom: 20px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .reservation-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    }
    .reservation-card h5 {
      font-size: 1.5rem;
      font-weight: 600;
      color: #343a40;
      margin-bottom: 10px;
    }
    .reservation-card p {
      font-size: 1rem;
      color: #6c757d;
      margin-bottom: 5px;
    }
    .reservation-user {
      background-color: #f8f9fa;
      padding: 10px;
      border-radius: 5px;
      margin-top: 15px;
    }
    .reservation-user p {
      margin: 0;
      color: #495057;
    }
    .reservation-actions .btn-cancel {
      margin-top: 15px;
      font-size: 0.9rem;
      padding: 8px 20px;
      border-radius: 30px;
      font-weight: 500;
      color: #dc3545;
      border: 1px solid #dc3545;
      background-color: transparent;
      transition: background-color 0.3s ease, color 0.3s ease;
    }
    .btn-cancel:hover {
      background-color: #dc3545;
      color: #ffffff;
    }

    /* Navbar */
    .navbar-brand img {
      margin-right: 10px;
    }
    .nav-link {
      font-size: 1rem;
      font-weight: 500;
      color: #ffffff !important;
      margin-right: 15px;
      transition: color 0.3s ease;
    }
    .nav-link:hover,
    .nav-link.active {
      color: #adb5bd !important;
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    @keyframes bounceIn {
      0% {
        transform: scale(0.5);
        opacity: 0;
      }
      60% {
        transform: scale(1.2);
        opacity: 1;
      }
      100% {
        transform: scale(1);
      }
    }
    @media only screen and (max-width: 425px) {
      .profile-header{
        display: flex;
        flex-direction: column;
      }
      .edit-profile-button{
        right: 0;
        top: 50%;
        transform: translateY(-50%);
      }
    }
    @media only screen and (max-width: 320px) {
      .edit-profile-button{
        padding: 5px 10px;
      }
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <a class="navbar-brand" href="/">
      <img
        src="/images/logo.png"
        width="30"
        height="30"
        alt="Logo"
      />
      Rent To Drive
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/vehicles">Available Vehicles</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/add-vehicle">Add Vehicle</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/reservations">Reservations</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/profile">Profile</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/logout">Logout</a>
        </li>
      </ul>
    </div>
  </nav>

  <!-- Profile Container -->
  <div class="container profile-container">
    <!-- Profile Header -->
    <div class="profile-header">
      <% if(user.profilePic){ %>
      <img
      src="/uploads/profile_pics/<%= user.profilePic %>"
        alt="Profile Picture"
      />
      <% }else{ %>
        <img src="/uploads/profile_pics/defalut.jpg" alt="Default Pic">
      <% } %>
      <h1><%= user.name %></h1>
      <p><%= user.email %></p>
      <p><%= user.phoneNumber %></p>
      <a
        href="/profile/edit"
        class="btn btn-outline-primary edit-profile-button"
        >Edit Profile</a
      >
    </div>

    <!-- Profile Details -->
    <div class="profile-details">
      <div class="detail-box">
        <h3>Vehicles Added</h3>
        <p><%= vehicles.length %></p>
      </div>
      <div class="detail-box">
        <h3>Reservations Made</h3>
        <p><%= reservations.length %></p>
      </div>
    </div>

    <!-- Vehicles Section -->
    <h2 class="section-title">Your Vehicles</h2>
    <div class="row">
      <% vehicles.forEach(function(vehicle) { %>
      <div class="col-md-6 col-lg-4 mb-4">
        <div class="card">
          <img
            src="/uploads/profile_pics/<%= vehicle.carImage %>"
            class="card-img-top"
            alt="<%= vehicle.make %> <%= vehicle.model %>"
          />
          <div class="card-body">
            <h5 class="card-title">
              <%= vehicle.make %> <%= vehicle.model %>
            </h5>
            <p class="card-text">
              $<%= vehicle.pricePerDay %> per day
            </p>
            <div class="card-actions">
              <a
                href="/vehicles/edit-vehicle/<%= vehicle._id %>"
                class="btn btn-edit"
                >Edit</a
              >
              <form action="/vehicles/<%= vehicle._id %>?_method=DELETE" method="post">
                <input type="submit" value="Delete" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this vehicle?');">
              </form>
             
            </div>
          </div>
        </div>
      </div>
      <% }) %>
    </div>

    <!-- Reservations Section -->
    <h2 class="section-title">Your Reservations</h2>
    <div class="row">
      <% reservations.forEach(function(reservation) { %>
      <div class="col-md-12 mb-4">
        <div class="reservation-card">
          <% if(reservation){ %>
          <h5><%= reservation.vehicle.make %> <%= reservation.vehicle.model %></h5>
          <p><strong>Start Date:</strong> <%= reservation.startDate.toDateString() %></p>
          <p><strong>End Date:</strong> <%= reservation.endDate.toDateString() %></p>
          <p><strong>Total Cost:</strong> $<%= reservation.totalCost %></p>
          <div class="reservation-user">
            <p>
              <strong>Booked By:</strong> <%= reservation.user.name %>
            </p>
            <p>
              <a href="mailto:<%= reservation.user.email %>" style="text-decoration: none; color: black;">
                <strong>Email: </strong><span style="color: #343a40;"><%= reservation.user.email %></span>
              </a>
            </p>
            <p>
              <a href="tel:<%= reservation.user.phoneNumber %>" style="text-decoration: none; color: black;">
                <strong>Phone Number: </strong><span style="color: #343a40;"><%= reservation.user.phoneNumber %></span>
              </a>
            </p>
            <% } %>
          </div>
          <div class="reservation-actions">
            <form action="/reservations/<%= reservation._id %>?_method=DELETE" method="POST" class="d-inline">
              <button type="submit" class="btn btn-danger">Cancel Reservation</button>
            </form>
          </div>
        </div>
      </div>
      <% }) %>
    </div>
  </div>

  <!-- Bootstrap JS and Dependencies -->
  <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/js/bootstrap.min.js"></script>
</body>
</html>
